# Environment variables for VPS deployment
# Copy to .env and adjust values
POSTGRES_PASSWORD=changeMeStrong
SECRET_KEY=replace-with-random-hex
# DATABASE_URL is constructed automatically in docker-compose for the web service
