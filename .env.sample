# Environment variables for production deployment
# Copy this file to .env and adjust values before deploying

# PostgreSQL Database Configuration
# Used by both the database service and the Flask app
POSTGRES_PASSWORD=changeMeStrongPassword123!

# Flask Application Configuration
SECRET_KEY=replace-with-random-secret-key-min-32-chars

# Flask Environment (development or production)
FLASK_ENV=production

# Database URL (automatically constructed in docker-compose.yml)
# For local development without Docker, use:
# DATABASE_URL=sqlite:///instance/data.db
# For production with Docker Compose, this is set automatically

# Optional: Custom domain for Caddy
# DOMAIN=yourdomain.com

# Note: Generate a secure SECRET_KEY with:
# python -c "import secrets; print(secrets.token_hex(32))"
